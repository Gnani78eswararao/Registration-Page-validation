# Registration-Page-validation
A responsive Student Registration Form built using HTML, CSS, and JavaScript. The project includes a clean registration interface with client-side form validation for name, password, email, phone number, gender, date of birth, languages known, and address.
